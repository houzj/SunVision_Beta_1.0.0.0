<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- ========== 颜色资源 ========== -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="#0066CC"/>
    <SolidColorBrush x:Key="PrimaryLightBrush" Color="#E8F4F8"/>
    <SolidColorBrush x:Key="SecondaryBrush" Color="#00CC99"/>
    <SolidColorBrush x:Key="WarningBrush" Color="#FF9900"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="#FF3333"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="#00CC99"/>
    <SolidColorBrush x:Key="InfoBrush" Color="#0066CC"/>

    <SolidColorBrush x:Key="BorderBrush" Color="#CCCCCC"/>
    <SolidColorBrush x:Key="BorderLightBrush" Color="#DDDDDD"/>
    <SolidColorBrush x:Key="BackgroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="BackgroundGrayBrush" Color="#F8F8F8"/>
    <SolidColorBrush x:Key="HeaderBrush" Color="#F0F0F0"/>

    <!-- ========== 字体资源 ========== -->
    <FontFamily x:Key="DefaultFont">Microsoft YaHei UI</FontFamily>
    <FontFamily x:Key="MonoFont">Consolas</FontFamily>

    <sys:Double x:Key="FontSizeSmall">10</sys:Double>
    <sys:Double x:Key="FontSizeNormal">12</sys:Double>
    <sys:Double x:Key="FontSizeMedium">13</sys:Double>
    <sys:Double x:Key="FontSizeLarge">14</sys:Double>
    <sys:Double x:Key="FontSizeXLarge">16</sys:Double>
    <sys:Double x:Key="FontSizeXXLarge">18</sys:Double>

    <!-- ========== 按钮样式 ========== -->
    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                        Margin="{TemplateBinding Padding}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#0055AA"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#004488"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="#CCCCCC"/>
                <Setter Property="BorderBrush" Value="#CCCCCC"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SecondaryButton" TargetType="Button">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                        Margin="{TemplateBinding Padding}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#D0E8F0"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="#F0F0F0"/>
                <Setter Property="BorderBrush" Value="#CCCCCC"/>
                <Setter Property="Foreground" Value="#999999"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ========== 文本框样式 ========== -->
    <Style x:Key="StyledTextBox" TargetType="TextBox">
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ========== 下拉框样式 ========== -->
    <Style x:Key="StyledComboBox" TargetType="ComboBox">
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <!-- ========== 侧边栏标题样式 ========== -->
    <Style x:Key="SideHeaderStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource FontSizeMedium}"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="#333333"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="Background" Value="{StaticResource HeaderBrush}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- ========== 节点样式 ========== -->
    <Style x:Key="NodeBorder" TargetType="Border">
        <Setter Property="CornerRadius" Value="5"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Cursor" Value="Move"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#000000" BlurRadius="3" ShadowDepth="2" Opacity="0.2"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ProcessingNode" TargetType="Border" BasedOn="{StaticResource NodeBorder}">
        <Setter Property="Background" Value="#E8F4F8"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
    </Style>

    <Style x:Key="PreprocessNode" TargetType="Border" BasedOn="{StaticResource NodeBorder}">
        <Setter Property="Background" Value="#FFF8E8"/>
        <Setter Property="BorderBrush" Value="{StaticResource WarningBrush}"/>
    </Style>

    <Style x:Key="DetectionNode" TargetType="Border" BasedOn="{StaticResource NodeBorder}">
        <Setter Property="Background" Value="#E8FFF8"/>
        <Setter Property="BorderBrush" Value="{StaticResource SecondaryBrush}"/>
    </Style>

    <Style x:Key="OutputNode" TargetType="Border" BasedOn="{StaticResource NodeBorder}">
        <Setter Property="Background" Value="#F8E8FF"/>
        <Setter Property="BorderBrush" Value="#9900CC"/>
    </Style>

    <!-- ========== TreeView样式 ========== -->
    <Style x:Key="StyledTreeView" TargetType="TreeView">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderLightBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- ========== Expander样式 ========== -->
    <Style x:Key="StyledExpander" TargetType="Expander">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="2"/>
    </Style>

</ResourceDictionary>
